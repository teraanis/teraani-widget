{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git diff --quiet 2>/dev/null && git diff --cached --quiet 2>/dev/null || echo '{\"systemMessage\": \"Codigo modificado detectado. Execute /security-review para checar vulnerabilidades antes do deploy.\"}'"
          }
        ]
      }
    ]
  }
}
